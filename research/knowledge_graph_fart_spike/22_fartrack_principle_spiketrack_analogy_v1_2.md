# FARTrack component → principle → SpikeTrack functional analogue

These are **functional analogies**, not proposed modules or a selected architecture. “Analogue” identifies where the same design question exists in SpikeTrack; it does not authorize an experiment or claim equivalence.

| FARTrack COMPONENT | GENERAL DESIGN PRINCIPLE | SPIKETRACK FUNCTIONAL ANALOGUE | Why functional | Must NOT be copied | Novelty collision | Evidence confidence |
|---|---|---|---|---|---|---|
| 15/10/6-layer fixed variants | Define a physically smaller operating graph rather than average conditional savings | Fixed reductions in SpikeTrack stage/block depth, later possibly width/dimension | Both change the always-executed structure and can be audited as an artifact | FARTrack layer indices, ViT block shape, or reported hardware speed | LiteTrack, MixFormerV2, CompressTracker, Hybrid-KD | HIGH for principle; LOW for untested SpikeTrack choice [E03, E05, E10-E12, E35] |
| Adjacent-depth TSSD | Preserve task-facing behavior when removing depth | Candidate preservation of SpikeTrack center-score, size, and offset behavior from a larger SpikeTrack teacher | These outputs are the recipient's actual tracking interface | Coordinate-vocabulary logits, FARTrack KL schedule, or assuming output KD alone is sufficient | MixFormerV2, UETrack, Hybrid-KD | HIGH for analogy; exact loss remains unresolved [E03, E05, E11, E33, E35] |
| Staged depth training | Stabilize compression by making the teacher/student transition explicit | Stage-wise SpikeTrack-compatible teacher/student training around fixed interface boundaries | SpikeTrack has named stage/cache/head interfaces that can define preservation checks | Randomly swapping spiking/dense stages without state-compatibility evidence | CompressTracker | MEDIUM [E03, E05, E10] |
| IFAS attention-derived template salience | Remove representation elements using target-relevant evidence | Candidate reduction at a SpikeTrack template-memory or stage representation boundary while preserving retrieval behavior | Both reduce template-derived work before repeated frames consume it | FARTrack attention score, 75% ratio, token indices, or one-stream sparse kernel | CPDATrack and Hybrid-KD; direct token pruning crowded | MEDIUM [E03, E05, E19, E35] |
| Persistent IFAS mask | Make a stable reduction decision once and amortize it | Persist a validated SpikeTrack representation/compression decision with the six template caches | Both avoid re-running selection when template evidence is stable | Copying the mask format or assuming a decision remains valid after update | STDTrack, AsymTrack, LiteTrack | MEDIUM [E03, E05, E09, E12, E21] |
| Historical template set | Retain complementary stable and recent target evidence | SpikeTrack fixed-first-template FIFO memory policy | Both combine initialization evidence with updated appearances | FARTrack crop schedule or trajectory-token semantics | STDTrack/MCITrack memory-quality work | HIGH for functional mapping [E03-E06, E16, E21] |
| Four command tokens/task distributions | Concentrate supervision at the task decision interface | SpikeTrack three-tower center/size/offset outputs and decoded-box behavior | Both directly determine localization rather than generic feature similarity | Autoregressive coordinate vocabulary or decoder | FastSeqTrack/SeqTrack head prior art; UETrack/Hybrid-KD task KD | HIGH [E03, E05, E14, E31, E33, E35] |
| Sparse inference path separated from training | Verify that a claimed reduction survives graph materialization | Released SpikeTrack template/search split, cache contract, and export-sensitive operator graph | A structural claim is only meaningful if the deployed artifact is actually smaller/executable | Inferring target-device speed from FLOPs, analytical energy, MX250, AGX, or CPU results | ABTrack's routing-overhead lesson and P06 engineering enablement | HIGH [E04, E06, E07, E24, E32] |

## Guardrail

No row selects a SpikeTrack stage, depth, width, timestep, distillation loss, or cache modification. The whole-MRM1 conditional-skip hypothesis is excluded: it is `DIAG_FAIL`, and its hold-out is consumed [E08].
